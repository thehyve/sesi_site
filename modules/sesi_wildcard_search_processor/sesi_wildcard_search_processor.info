name = Sesi Wildcard search processor
description = This search processor is designed to replace the standard Tokenizer processor from the search api. This will enable the use of * and ? wildcards.
core = 7.x
version = "7.0"
package = sesi
project = sesi
dependencies[] = search_api
